栈的实现：
1.基于数组的顺序栈+
2.基于链表的链式栈。

1.需要一个大小为n(Maxsize)的数组。
在入栈和出栈的过程中，只需要一两个临时变量存储空间，所以空间复杂度是O(1)；
只涉及个别数据的操作，因此时间复杂度也为O(1)。
空间复杂度是指除了原本的数据存储空间外，（因此存储数据的大小为n的数组不影响）算法运行还需要的额外的存储空间。


1 .是一个固定大小的栈。

3.支持动态扩容的栈——底层：支持动态扩容的数组。(用Arrays的copyOf方法，可直接返回复制后的数组)

栈的应用：1.函数的调用。
          2.在表达式求值中的应用。数字栈+符号栈。
          3.括号匹配。用栈保存未匹配的左括号，从左到右依次扫描字符串，当扫描到右括号时，从栈顶取出一个左括号，如果能够匹配，使左括号出栈，继续扫描字符串；如果不能匹配，则说明该式不符合要求。